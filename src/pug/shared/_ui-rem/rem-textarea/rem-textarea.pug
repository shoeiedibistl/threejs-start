mixin rem-textarea
  .textarea-ui.custom-placeholder(data-input-parent='')
    textarea(data-input='', required='', name=name ? name : '')&attributes(attributes)
    .custom-placeholder__service
      span
        | #{ attributes.placeholder }
      +reset.custom-placeholder__reset
